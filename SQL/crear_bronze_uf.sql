CREATE TABLE IF NOT EXISTS bronze_uf (
    fecha DATE NOT NULL,
    uf DECIMAL NOT NULL
);

CREATE TABLE IF NOT EXISTS silver_uf (
    id SERIAL PRIMARY KEY,
    fecha DATE NOT NULL,
	fecha_carga TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    uf DECIMAL NOT NULL
);